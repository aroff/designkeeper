[target.x86_64-unknown-linux-musl]
# perl is required by openssl-sys to build vendored OpenSSL from source.
pre-build = ["apt-get install -y --no-install-recommends perl"]

[target.x86_64-unknown-linux-musl.env.set]
OPENSSL_STATIC = "1"
OPENSSL_VENDORED = "1"
